
<footer>
    FOOTER
    <div>Hello</div>
    <div>Goodbye</div>
    <!--<div class="grid-container">
        <div class="grid-row">
            <div class="grid-item">
                <div class="footer-title">Recent Posts</div>
                    {% for post in collections.post.slice(collections.post.length-4,collections.post.length) | reverse %}
                        <div class="media">
                            <a href="{{ post.url }}">
                                <img class="d-flex mr-3" 
                                    src="{{post.url}}{{post.data.imageThumbnail}}" 
                                    alt="{{post.url}}{{post.data.imageThumbnail}}" 
                                    style="max-width:150px;"
                                    width="150px"
                                />
                            </a>
                            <div class="media-body">
                                <p>
                                    <a href="{{ post.url }}">{{ post.data.title }}</a>
                                </p>
                                <span>{{post.data.date.toISOString().split('T')[0]}}</span>
                            </div>
                        </div>
                    {% endfor %}
                </div>
                <div class="grid-item">
                    <div class="footer-title">Want to hear more from me? Say thanks? Or have a discussion?
                        
                    {% if site.patreon %}
                    <p>{{site.patreon.footerMessage}}</p>
                    <a class="nav-link" href="{{site.patreon}}"><img src="/assets/img/patron.png" alt="Become a Patron!" width="50px" height="213px" style="height:50px"></a>
                    {% endif %}
                    </div>
                </div>
            </div>
        </div>
        <div class="grid-row">
            <div class="copyright-txt text-center">
                {{site.author}} © {{site.lastBuildYear}} · 
                <a href="/index.xml">RSS <i class="fa fa-rss" aria-hidden="true"></i></a>
                <a href="https://twitter.com/{{site.twitter}}">Twitter</a>
                <a href="{{site.github}}">GitHub</a>
                <a href="{{site.linkedin}}">LinkedIn</a>
            </div> 
        </div>
        <div class="grid-row">
            <div class="copyright-txt text-center">
                Updated on: {{site.lastBuildDate}}
            </div>
        </div>
    </div>-->
</footer>